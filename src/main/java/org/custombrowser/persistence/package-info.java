/**
 * JDBC connection and repository boundaries.
 *
 * <p>Database DDL is intentionally excluded from Java and belongs in the
 * Docker PostgreSQL initialization SQL directory.</p>
 */
package org.custombrowser.persistence;
